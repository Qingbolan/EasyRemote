# EasyRemote并发流式任务案例依赖项

# 基础依赖
easyremote>=0.1.2.2.5.1
numpy>=1.21.0
rich>=13.0.0

# 可选依赖（用于扩展功能）
psutil>=5.8.0  # 系统监控
asyncio-throttle>=1.0.0  # 流量控制（可选）

# 开发依赖（可选）
pytest>=6.0.0
pytest-asyncio>=0.18.0 